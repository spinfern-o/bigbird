PHOTOFORGE ROADMAP - WORK SPLIT INTO TWO PARALLEL TRACKS
========================================================

The remaining work is split into two tracks that can be built at the same time.
Each track mostly owns its own files, so the two developers rarely edit the same
code. Where they must touch shared files, follow the "Shared files" rules below.

  TRACK A - Selections & AI
  TRACK B - Pro Editing & Lightroom Features

Workflow for everyone:
  * One branch + one pull request per numbered task (e.g. branch "a2-ai-framework").
  * Keep PRs small. Rebase on main before opening a PR, and after the other
    track merges something.
  * Every new control gets a plain-English tooltip and a status-bar hint.
    PhotoForge is for beginners.
  * Every change must be undoable (use doc.push_undo before changing state).

For claude:
the login doesnt save when i enter my password, when i finish registering the tab should automatically close and the app should automatically be connected and know that i have signed up, signign me in instantly to my own dashboard/account. check if vercel is connected properly to supabase propery and if running locally is fine. run your own tests and push through github instead of vercel. look again at the roadmap and focus on a6 after debugging. 

--------------------------------------------------------------------------------
TRACK A - SELECTIONS & AI
--------------------------------------------------------------------------------
Owns:  app/selection.py (new), app/ai/ (new package), app/ai_dialogs.py (new)

~~A1. Selection system~~  [DONE]
    ~~Document selection mask, Marquee, Ellipse, Lasso, Magic Wand, marching ants,
    Select menu/tab/context menu, selection-aware Brush/Eraser/Filters, Delete,
    and Ctrl+J Layer via Copy are complete.~~

A2. AI framework  [MOSTLY DONE]
    - ~~app/ai/base.py unified LocalBackend / CloudBackend interface.~~ [DONE]
    - ~~Local ONNX Runtime path with GPU acceleration + CPU fallback.~~ [DONE]
    - ~~Cloud NVIDIA path through authenticated phrame.tech/api/edit proxy.~~ [DONE]
      The NVIDIA key stays server-side in Vercel/hosting environment variables; it is
      intentionally never stored in the desktop app.
    - ~~AI Settings for local vs optional Brev acceleration.~~ [DONE]
    - ~~Model downloader with progress + cancel in %LOCALAPPDATA%\PhotoForge\models.~~ [DONE]
    - ~~Background-thread AI runner with progress UI.~~ [DONE]
    - cloud_notice() exists; A6 still needs to show the confirmation in its user-facing UI.

~~A3. Remove Background (local: BiRefNet-lite)~~  [DONE]
    ~~Refine Outline, editable points/lines, and new-layer output are complete.~~

~~A4. Click-to-Select (local: SAM 2.1 Tiny)~~  [DONE]
    ~~Object selection integrates with the normal A1 selection system.~~

~~A5. Remove Object~~  [DONE, redesigned]
    ~~SAM selection → transparent removal layer → Refine Removal is complete.~~
    Filling the transparent gap intentionally remains part of A6 cloud generative fill.

A6. Describe Your Edit / Generative Fill (cloud: NVIDIA - Qwen-Image-Edit, FLUX.2-klein 4B)
    - Includes "Fill removed area": fill the transparent gap left by A5.
    - Note: NVIDIA's Nemotron models are text / vision-language models; they can describe
      an image but cannot generate pixels, so they are not used for filling.
    - Text box: "make it sunset", "remove the people", "add snow".
    - With a selection: only the selected area is regenerated.
    - Result on a new layer; offer 2-3 variations to pick from.

A7. AI Upscale 2x / 4x (Real-ESRGAN: realesr-general-x4v3 fast, x4plus best quality)

~~A8. Open-source licenses screen~~  [DONE]
    ~~Help > Open-source Licenses lists the included models/libraries and license info.~~

APPROVED AI MODELS (all free to download and allowed in commercial products)
    Licenses verified on each model's official page, September 2026.
    RULE: only use models whose license allows commercial use. Before adding any
    new model, check its license and add it to this table.

    Feature             Model                         License       Where to get it
    ------------------  ----------------------------  ------------  ------------------------------------------
    Remove background   BiRefNet-lite (44M params)    MIT           huggingface.co/onnx-community/BiRefNet_lite-ONNX
    Click-to-select     SAM 2.1 Tiny (39M params)     Apache 2.0    huggingface.co/onnx-community/sam2.1-hiera-tiny-ONNX
                                                                    (converted from facebook/sam2.1-hiera-tiny)
    Remove small areas  MI-GAN                        MIT (code +   github.com/Picsart-AI-Research/MI-GAN
                                                      weights)      (official ONNX provided)
    Remove large areas  LaMa (big-lama)               Apache 2.0    github.com/advimman/lama
    Upscale             Real-ESRGAN                   BSD-3-Clause  github.com/xinntao/Real-ESRGAN
                        (realesr-general-x4v3, x4plus)
    Cloud: edit         Qwen-Image-Edit               Apache 2.0    via NVIDIA build.nvidia.com
    Cloud: edit/gen     FLUX.2-klein 4B               Apache 2.0    via NVIDIA build.nvidia.com

    DO NOT USE (non-commercial licenses): RMBG-2.0 (CC BY-NC 4.0), CodeFormer,
    FLUX.1-dev, FLUX.2-klein 9B. For face restoration, check GFPGAN (Apache) first.

    Costs: the models themselves are free, with no fees or royalties; the only
    obligation is attribution (A8). Running them locally costs nothing. The cloud
    features cost money only for the NVIDIA hosting: free for development, paid
    at scale. The same Apache models could also be self-hosted later.

    Before picking each default, test both candidates on a few real photos with
    the GPU disabled (simulating a weak laptop). Default = the best result that
    finishes in about 3 seconds. Offer a "Fast / Best quality" setting.


--------------------------------------------------------------------------------
TRACK B - PRO EDITING & LIGHTROOM FEATURES
--------------------------------------------------------------------------------
Owns:  app/adjustments.py, app/panels.py (AdjustPanel part), app/curves.py (new),
       app/library.py (new), app/retouch.py (new), packaging files

~~B1. Color Mixer (HSL)~~  [DONE]
    ~~8-color Hue/Saturation/Luminance mixer, UI, reset behavior, and presets are complete.~~

~~B2. Tone Curve~~  [DONE]
    ~~Draggable curve widget (RGB + separate R/G/B) drawn over the live histogram,
    plus the simple "Lights / Darks" region sliders. Click the line to add a point,
    drag to bend it, right-click a point to delete it. Monotone cubic interpolation
    keeps the curve from overshooting. app/curves.py.~~

~~B3. Layer Masks~~  [DONE]
    ~~Layer.mask (HxW uint8); compositing, crop/rotate/flip/resize and .pforge projects
    all respect it. Layers panel has the mask thumbnail, Add Mask, Mask from Selection,
    Invert, Delete, Apply and a "Paint on the mask" switch (Brush hides, Eraser brings
    back, with a coloured overlay while you paint). Same commands in Layer > Layer Mask.~~

~~B4. Retouch tools~~  [DONE]
    ~~Spot Healing Brush, Remove Blemishes, Smooth Skin, Reduce Redness, Heal Selection,
    Clone Stamp (S, Alt+click to set the source, aligned/fixed source, source marker)
    and Remove Red Eye (whole photo, or only inside a selection).~~

~~B5. Local adjustments (Lightroom-style)~~  [DONE]
    ~~Graduated filter (G) and Radial filter (U): drag on the photo, then use the new
    Local tab for exposure, contrast, highlights, shadows, whites, blacks, temperature,
    tint, vibrance, saturation, clarity, dehaze and sharpening inside that area only,
    plus Softness and "flip which side is affected". Stored in canvas fractions so the
    preview and the export match. app/local_adjust.py, app/local_panel.py.~~

~~B8. Match Style from a Photo~~  [DONE]
    ~~Pick a photo whose look you want; PhotoForge histogram-matches each colour channel
    and fits the result onto the Tone Curves + Saturation, so the whole look stays
    visible and editable instead of being baked into pixels. Before/after/reference
    preview with a Strength slider. Image menu (Ctrl+Shift+M) and the Adjust tab.
    app/matchstyle.py.~~

B6. Library / multiple photos
    - Folder browser with a thumbnail filmstrip; star ratings.
    - Copy / paste adjustment settings between photos.
    - Batch export (same settings, many photos).

B7. App polish & shipping  [PARTLY DONE]
    - ~~Remember window layout, recent files, last folder (QSettings).~~ [DONE]
    - History panel (click any step to jump back).
    - App icon, proper .exe and installer (PyInstaller + Inno Setup).
    - Crash-safe autosave of the open project.


--------------------------------------------------------------------------------
SHARED FILES - RULES TO AVOID CONFLICTS
--------------------------------------------------------------------------------
These files are used by both tracks. Keep edits to them small and additive
(add new methods/menu entries; don't reorganise existing code in the same PR):

  app/document.py     A adds `selection`; B adds `Layer.mask`. Coordinate on
                      compositing: B owns composite(); A only reads the selection.
  app/canvas.py       Each new tool goes in its own clearly named methods.
                      Tool ids: A uses "select_rect", "select_ellipse", "lasso",
                      "ai_select"; B uses "clone", "heal", "gradient", "radial".
  app/main_window.py  Add menus/actions through small helper methods
                      (e.g. _build_ai_menu, _build_retouch_tools) rather than
                      editing existing blocks.
  app/icons.py        Append new icon functions only.
  requirements.txt    Append only. (A adds onnxruntime-directml, requests.)

CURRENT NEXT PRIORITIES
-----------------------
Track A:
  1. A6 Describe Your Edit / Generative Fill.
     The authenticated Vercel/NVIDIA backend groundwork already exists; build the actual
     prompt/selection UI, cloud notice, Fill Removed Area flow, new-layer result, and
     2-3 variations.
  2. A7 AI Upscale 2x / 4x with Real-ESRGAN.

Track B:
  1. B6 Library / multiple photos (folder browser, ratings, copy/paste settings,
     batch export).
  2. Finish B7 with the History panel, packaging/installer, and crash-safe autosave.

Recommended immediate parallel work:
  Track A -> A6
  Track B -> B6

